# Examples

```bash
➜  examples go build
➜  examples ./examples -help
Usage of ./examples:
  -n int
        number of row (or column) of the positive-definite matrix A (default 10)
  -tol float
        stopping criterion (default 1e-06)
➜  examples ./examples -n 23
```
